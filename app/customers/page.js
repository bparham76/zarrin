import { OurCustomers } from "@/components/pages";
import globalMetadata from "../globalMetadata";

const page = () => {
    return <OurCustomers />;
}

export default page

export const metadata = globalMetadata('بیلبورد زرین | مشتریان');