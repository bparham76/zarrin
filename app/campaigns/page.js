import { AdvCamps } from "@/components/pages";
import globalMetadata from "../globalMetadata";

export const metadata = globalMetadata('بیلبورد زرین | کمپین های تبلیغاتی')

const page = () => {
  return <AdvCamps />
}

export default page