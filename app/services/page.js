import { ServicesPage } from "@/components/pages";

import globalMetadata from "../globalMetadata";

export const metadata = globalMetadata('بیلبورد زرین | خدمات');

const page = () => {
    return <ServicesPage />;
}

export default page